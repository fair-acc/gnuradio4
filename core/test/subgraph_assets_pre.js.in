// Auto-generated by CMake from subgraph_assets_pre.js.in — do not edit.
// Runs on the Node.js main thread before the WASM module starts.

XMLHttpRequest = require('xhr2');

var http = require("http");
var fs   = require("fs");
var path = require("path");

http.createServer(function(req, res) {
    fs.readFile(path.join("@ASSETS_DIR@", req.url), function(err, data) {
        if (err) { res.writeHead(404).end(); } else { res.writeHead(200).end(data); }
    });
}).on("error", function(e) {
    if (e.code !== "EADDRINUSE") { throw e; }
}).listen(@HTTP_SERVER_PORT@, "127.0.0.1").unref();
