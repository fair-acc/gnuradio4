definition_metadata:
  block_type: MyGammaBlock
blocks:
  - id: SUBGRAPH
    parameters:
      name: chained_multiplier_gamma
    graph:
      blocks:
        - id: good::multiply<float64>
          parameters:
            name: multiplier1
        - id: good::multiply<float64>
          parameters:
            name: multiplier2
      connections:
        - [multiplier1, 0, multiplier2, 0]
      exported_ports:
        - [multiplier1, INPUT, in, in]
        - [multiplier2, OUTPUT, out, out]
