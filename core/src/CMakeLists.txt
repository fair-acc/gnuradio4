if(ENABLE_EXAMPLES)
  add_executable(main main.cpp)
  target_include_directories(main PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../test)
  target_link_libraries(main PUBLIC gnuradio-options gnuradio-core magic_enum)
endif()
